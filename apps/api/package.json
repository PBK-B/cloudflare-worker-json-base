{
	"name": "@jsonbase/api",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"type-check": "tsc -p tsconfig.json --noEmit",
		"dev": "wrangler dev --config wrangler.dev.toml --port 8788",
		"test": "npx jest --config jest.config.json"
	},
	"dependencies": {
		"@jsonbase/shared": "0.0.0"
	}
}